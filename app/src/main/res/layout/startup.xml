<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:layout_margin="@dimen/space_std_w"
    android:orientation="vertical" >

    <TextView
        android:id="@+id/textView_startup"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="15dip"
        android:text="@string/app_starting" />

    <TextView
        android:id="@+id/textView_ipconnect"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/startup_enter_ip" />

    <EditText
        android:id="@+id/editText_ConnectIP"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:ems="10"
        android:inputType="text" >

        <requestFocus />
    </EditText>

    <ListView
        android:id="@+id/listView_startup_found"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_weight="1" >

    </ListView>

    <ScrollView
        android:id="@+id/scrollView1"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:layout_weight="1" >

        <TextView
            android:id="@+id/textView_startup_infos"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_marginTop="@dimen/space_std_h"
            android:text="@string/startup_infos"
            android:textSize="@dimen/font_size_med" />

    </ScrollView>

</LinearLayout>
