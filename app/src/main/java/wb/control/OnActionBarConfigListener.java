package wb.control;

public interface OnActionBarConfigListener {

    void OnActionBarItemConfig(int itemID, boolean visible);

    //Callback für Fragments, die ein ActionBar Item hinzufügen oder entfernen wollen

/*
 * itemID: in FAct_control definiert
 * visible: true -> wird in ActionBar wird angezeigt, false -> wird versteckt
 */


}
